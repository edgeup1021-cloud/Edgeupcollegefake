export enum UserRole {
  STUDENT = 'student',
  TEACHER = 'teacher',
  ADMIN = 'admin',
}

export enum UserType {
  STUDENT = 'student_users',
  TEACHER = 'teacher_users',
  ADMIN = 'mgmt_admin_users',
}
